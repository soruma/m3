en:
  activerecord:
    models:
      account:
        one: account
        other: accounts
      event:
        one: event
        other: events
      history:
        one: history
        other: histories
      use:
        one: use
        other: uses
    attributes:
      account:
        use_id: use
        name: name
      event:
        date_of_onset: date of onset
        name: name
        remarks: remarks
      history:
        date_of_onset: date of onset
        account_id: account
        price: price
      use:
        name: name
